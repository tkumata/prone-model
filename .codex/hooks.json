{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "make check && make build",
            "statusMessage": "[Codex Hooks] Running make check and build..."
          }
        ]
      }
    ]
  }
}
