# TODO

## 優先度A

- [x] `README` と `docs` の内容を実装と一致させ続ける
- [x] 実機で Wi-Fi 接続から Web UI 表示まで確認する
- [x] 実機で撮影、保存、エクスポート、リセットを確認する
- [x] `metadata.csv` の列定義を固定する
- [ ] `metadata.csv` の後方互換ルールを決める
- [ ] `subject_id` と `session_id` の運用台帳を作る
- [ ] ラベル監査フローを決める
- [ ] 分割エクスポートの取得手順を確定する
- [ ] PC 側で `96 x 96` 前処理を適用した学習データ生成手順を文章化する
- [ ] `float` モデル、`ONNX`、`ESP-DL` 形式モデルの成果物保存先を決める
- [ ] `board_name=Freenove ESP32-S3 WROOM CAM` 以外を除外する監査手順を決める
- [ ] 判定閾値の固定手順を決める
- [ ] 実機確認用集合の分離ルールを台帳化する
- [ ] 実機合格基準の計測手順を文章化する

## 優先度B

- [ ] 分割エクスポート取得スクリプトを作る
- [ ] PC 側の `train / val / test` 一覧生成スクリプトを作る
- [ ] 保存失敗時のログを整理する
- [ ] SD カード残容量表示を追加する
- [x] 撮影成功または失敗の画面フィードバックを追加する
- [ ] 撮影成功音を追加する
- [ ] 学習利用可否の変更履歴管理方法を決める
- [ ] PC 量子化参照と `.espdl` 実機結果の比較スクリプトを作る

## 優先度C

- [x] 設置位置 ID、照明条件 ID などの入力 UI を追加する
- [ ] 画像のブレ・露出失敗を簡易検知する
- [ ] PC 側のデータ検品ツールを追加する
- [ ] 学習用ディレクトリ分割ツールを追加する
- [ ] `ESP-DL` 量子化後モデルの評価レポート自動生成を追加する
- [ ] 実機確認用集合の自動評価レポート生成を追加する
