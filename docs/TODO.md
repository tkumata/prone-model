# TODO

本プロジェクトの主線は、`Freenove ESP32-S3 WROOM CAM` で学習データを収集し、PC で学習と変換を行い、最終的に `.espdl` 形式のモデルを実機へ導入することである。

そのため、TODO は機能単位ではなく、収集から `.espdl` 作成、実機導入までの流れに沿って管理する。

## フェーズ1: 学習データ収集システム

- [x] Wi-Fi `STA` 接続と Web UI 表示を実装する
- [x] `GET /stream` によるライブストリームを実装する
- [x] `POST /api/capture` による撮影と保存を実装する
- [x] `dataset/images/*.jpg` と `dataset/metadata.csv` の保存を実装する
- [x] `GET /api/export/manifest`、`GET /api/export/metadata`、`GET /api/export/image` による分割エクスポートを実装する
- [x] `POST /api/reset` によるデータセット初期化を実装する
- [x] 必須メタデータ入力とバリデーションを実装する
- [x] 撮影成功または失敗の画面フィードバックを実装する
- [x] 実機で Wi-Fi 接続から Web UI 表示まで確認する
- [x] 実機で撮影、保存、エクスポート、リセットを確認する

## フェーズ2: データ仕様と運用固定

- [x] `metadata.csv` の列定義を固定する
- [x] 分割エクスポートの取得手順をドキュメント化する
- [x] `96 x 96` 前処理を前提にした学習データ生成手順をドキュメント化する
- [x] `float` モデル、`ONNX`、`ESP-DL` 形式モデルの成果物保存先を固定する
- [x] `board_name=Freenove ESP32-S3 WROOM CAM` 以外を除外する監査方針をドキュメント化する
- [x] 判定閾値の固定方針をドキュメント化する
- [x] `metadata.csv` の後方互換ルールを決める
- [x] `subject_id` と `session_id` の運用台帳を作る
- [x] ラベル監査フローを決める
- [x] 学習利用可否の変更履歴管理方法を決める
- [x] 実機確認用集合の分離ルールを台帳化する
- [x] 実機合格基準の計測手順を文章化する

## フェーズ3: PC 学習パイプライン

- [x] Mac 向け PC モデル生成パイプラインの初期実装を追加する
- [x] `metadata.csv` の必須列検証とデータ監査を実装する
- [x] `is_usable_for_training=1` かつ `exclude_reason=\"\"` の学習対象抽出を実装する
- [x] `subject_id` 単位の `train / val / test` 分割を実装する
- [x] `splits/train.csv`, `splits/val.csv`, `splits/test.csv` の出力を実装する
- [x] `96 x 96`、RGB 化、`float` モデル学習を実装する
- [x] 検証集合での判定閾値選択と `reports/threshold.json` 出力を実装する
- [x] `reports/metrics.json` と `dataset_audit.json` の出力を実装する
- [x] `onnx/model.onnx` の出力を実装する
- [ ] 学習用ディレクトリ分割ツールを追加する
- [ ] PC 側の量子化参照評価処理を実装する

## フェーズ4: `.espdl` 生成

- [x] `--espdl-converter-command` による外部変換コマンド連携を実装する
- [x] 変換結果を `reports/conversion.json` へ保存する
- [ ] 標準の `ESP-DL` 変換コマンドと実行手順を確定する
- [ ] `.espdl` 生成後のクラス順、入力サイズ、色順の検証手順を確立する
- [ ] PC 量子化参照と `.espdl` の一致確認手順を確立する
- [ ] `ESP-DL` 量子化後モデルの評価レポート自動生成を追加する

## フェーズ5: 実機導入

- [ ] `.espdl` を読み込む実機推論機能を実装する
- [ ] PC 側前処理と同一条件の実機前処理を実装する
- [ ] 実機推論結果と PC 量子化参照結果の比較スクリプトを作る
- [ ] 実機確認用集合の自動評価レポート生成を追加する
- [ ] 実機受け入れ試験を実施する

## フェーズ6: 運用改善

- [ ] 分割エクスポート取得スクリプトを作る
- [ ] 保存失敗時のログを整理する
- [ ] SD カード残容量表示を追加する
- [ ] 撮影成功音を追加する
- [ ] `status` の自動更新頻度と表示項目を見直す
- [ ] 画像のブレ・露出失敗を簡易検知する
