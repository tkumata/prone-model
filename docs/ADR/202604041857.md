# ADR

- 年月日時分: `202604041857`
- 決定した理由:
  - 学習データ収集時の bbox は固定ガイドではなく、顔を検出できた事実をそのままオペレータへ返す必要がある
  - ライブ表示と見た目を合わせるため、MJPEG ストリームで送るフレームから一定間隔で顔検出し、その結果を Web UI に返す
  - 顔検出に失敗しても撮影機能とストリーム自体は止めず、bbox だけを非表示にする
